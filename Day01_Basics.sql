-- Day 01: SELECT and FROM
-- Table used: students(id, name, age, course, city)

-- 1. Display all columns and all records
SELECT * FROM students;

-- 2. Show only the name and course of all students
SELECT name, course FROM students;

-- 3. Display only the city column
SELECT city FROM students;

-- 4. Show id, name, and age of all students
SELECT id, name, age FROM students;

-- 5. Fetch all records using SELECT *
SELECT * FROM students;

-- 6. Display the name and city of every student
SELECT name, city FROM students;

-- 7. Show all data from students table (full syntax)
SELECT * FROM students;

-- 8. Display only the course column
SELECT course FROM students;

-- 9. Show id and name of all students
SELECT id, name FROM students;

-- 10. Write one query that shows everything from the table
SELECT * FROM students;
